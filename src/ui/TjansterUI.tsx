import * as React from 'react';
import { styled } from '../stitches.config';

export const TjanstImg = styled('img', {
  width: '100%',
  margin: '-2px -3px',

  ':hover': {
    // textDecorationColor: '$green',
    // textDecorationStyle: 'solid',
    color: '#E996AC',
    display: 'none',
  },
});
